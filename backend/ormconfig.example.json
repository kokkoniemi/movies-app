{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "password": "supersecret",
  "database": "movies",
  "entities": ["./src/models/*.ts"],
  "logging": true,
  "migrations": ["./src/migrations/*.ts"],
  "cli": {
      "migrationsDir": "./src/migrations"
  }
}
